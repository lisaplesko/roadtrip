roadtrip
========

LearnLaunch14

To run the app:

gem install sinatra
gem install rubygems
gem install sinatra
gem install mongo
gem install json
gem install dotenv
gem install rack
gem install sinatra/reloader

then:
ruby server.rb
